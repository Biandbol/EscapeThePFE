using UnityEngine;

public class gearroller : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

 
 
 
public void roll(float speed){
transform.Rotate(0,0,speed);

}}
